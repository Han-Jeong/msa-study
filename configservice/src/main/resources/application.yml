server:
  port: 8888

spring:
  application:
    name: config-service
  #  profiles:
  #    active: native
  cloud:
    config:
      server:
        git: #default
          uri: https://github.com/Han-Jeong/spring-cloud-config.git
          default-label: main
          username: Han-Jeong
          password:
        bootstrap: true
#  rabbitmq:
#    host: 127.0.0.1
#    port: 5672
#    username: guest
#    password: guest

management:
  endpoints:
    web:
      exposure:
        include: health, busrefresh, refresh, metrics